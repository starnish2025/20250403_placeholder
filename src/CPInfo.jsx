export default function CPInfo(props) {
   const userID = [];
   const id = [];
   return (
      <div className="info">
         <p>
            {props.userID} {id}
         </p>
      </div>
   );
}
